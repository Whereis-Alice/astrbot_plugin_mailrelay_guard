# 更新日志

此文件记录 MailRelay Guard 的重要变更。

## v1.4.0 - 2026-08-13

- 新增邮件附件能力：爱丽丝可以附加当前 QQ 消息中的图片/文件，也可以附加当前会话工作区或 AstrBot 临时目录中的文件。
- 支持读取引用回复中的媒体；同一媒体重复出现会自动去重，NapCat 不需要新增 URL、Token 或额外配置。
- HTML 模板支持 `{{image_1}}`、`{{image_2}}` 等占位符，图片会转换为标准 CID 内嵌资源；未引用的图片仍作为普通附件发送。
- 新增附件数量、单文件大小、总大小、读取超时、危险扩展名和来源开关，默认参数已完整填写并在服务端强制校验。
- 邮件中心详情显示附件数量、总大小、CID 图片数量和文件名；本地历史数据库自动升级到 v2，不保存附件内容本身，默认元数据历史也不保存文件名。
- 保持原有收件人权限：普通用户只能给自己发，给主人和第三方代发仍需要原有的身份白名单与 AstrBot 管理员双重校验。
- 兼容 AstrBot 4.27.2 的实际会话工作区接口；工作区附件不再因新版本辅助函数缺失而被误判为不可用。
- 修复 HTML 模板含 `{{image_N}}` 但没有对应图片时可能继续投递的问题；现在会明确拒绝缺图模板。
- 支持在计算机工具运行时关闭的情况下附加当前事件已登记的临时文件，同时继续拒绝未登记的任意绝对路径。

## v1.3.4 - 2026-08-09

- 优化邮件列表的快速选择：标题独占首行并使用完整可用宽度，收件人和时间移到下一行；鼠标悬停可查看未截断的完整标题。
- 移除列表中的 SMTP 状态和 HTML 格式标签，避免它们挤占标题；完整投递结果、邮件格式和收件人状态仍保留在邮件详情中。
- 新增“全屏阅读”：优先请求浏览器原生全屏，并同时隐藏插件导航、列表和普通操作栏；若 AstrBot 的嵌入页面禁止原生全屏，则自动使用可退出的沉浸阅读模式。

## v1.3.3 - 2026-08-09

- 重新设计桌面端邮件中心为阅读器布局：进入邮件中心即使用 70px 导航轨，邮件列表变为紧凑的定位栏，右侧阅读器获得绝大多数页面宽度和可用高度。
- 将筛选和归档说明收进默认折叠面板；列表上方只保留文件夹、搜索、记录数量和刷新，避免控制项占据邮件阅读空间。
- 选中邮件后新增“专注阅读”切换：可临时完全收起列表，让 HTML 安全预览、纯文本或源码占满工作区；再次点击即可恢复列表。
- 压缩邮件标题、投递摘要和阅读标签的垂直高度，正文预览更靠近页面顶部。HTML 沙箱、二次清洗、无网络加载和 SMTP 权限规则均未改变。

## v1.3.2 - 2026-08-09

- 重构桌面端邮件中心为全高阅读工作台：文件夹、搜索、筛选和邮件列表固定在左侧窄栏，选中的邮件详情从工作区顶部延伸到底部。
- HTML 安全预览不再位于筛选栏和列表之后；正文阅读区会占据右侧主要高度，投递信息保持折叠，仍可按需展开。
- 默认 HTML 阅读尺寸调整为 120%，并继续提供 90% 至 145% 的本地预览调节，不改变归档内容或实际投递邮件。

## v1.3.1 - 2026-08-09

- 将邮件中心调整为阅读优先：桌面端邮件列表收窄为导航，选中邮件的正文和预览成为主要工作区。
- 将投递模式、SMTP 结果、收件人状态等技术详情改为按需展开，减少对正文阅读的干扰。
- 为 HTML 安全预览增加阅读尺寸调节；该控制只影响 Dashboard 显示区域，不会修改已归档邮件或重新投递。

## v1.3.0 - 2026-08-09

- 新增 AstrBot Dashboard 插件 Page“邮件中心”，提供运行概览、发件箱、本地投递副本、投递异常、HTML 安全预览和非敏感运行设置。
- 新增独立 SQLite 本地邮件历史，默认只保存脱敏投递元数据、模式、格式、状态、收件人数与脱敏地址；主题、正文、完整邮箱地址和原始聊天身份默认不保存。
- 新增可选的 `mail_history_store_content`。明确开启后，仅在 SMTP 至少接受一位收件人时保存主题、纯文本正文和已经清洗的 HTML；绑定验证码邮件始终不保存。
- 新增本地投递副本的已读、收藏和归档状态。该副本明确表示 SMTP 已接受，不是 IMAP/POP3 真实收件箱，也不表示邮件已最终送达或被阅读。
- HTML Dashboard 预览在发送清洗之外再次按更严格规则处理，禁用链接、远程图片和主动内容，避免打开历史记录时触发外部访问。
- 邮件中心只允许修改非敏感运行配置；SMTP 主机、账号、授权码、发件地址和发件人名称继续只能通过 AstrBot 原生配置面板管理，避免凭据经页面接口暴露。
- 将最低 AstrBot 版本提高至 `4.27.2`，以支持受认证的插件 Pages 和邮件中心接口。

## v1.2.0 - 2026-08-09

- 新增三种受原有收件人权限边界约束的 HTML 模板邮件工具：给主人、发给自己和管理员代发。
- 新增基于 `nh3` 与 `tinycss2` 的最终投递前 HTML 清洗，删除脚本、表单、事件属性、危险 URL、相对 URL、外链 CSS 和高风险 CSS 函数。
- 新增 HTML 功能、严格清洗、HTTPS 链接、远程图片、远程图片精确域名白名单和 HTML 源码长度配置项，默认保持关闭或拒绝优先。
- HTML 邮件使用 `multipart/alternative` 同时发送清洗后的 HTML 与由其生成的纯文本备用内容。
- 增加 HTML 清洗绕过、图片域名伪装、MIME 双版本邮件、工具注册和自助收件人隔离回归测试。

## v1.1.2 - 2026-08-09

- 修复 JSON 配置中的显式 `null`：敏感布尔开关不会再回落到开放默认值，`smtp_host` 和 QQ 邮箱推导域名也不会在显式清空后静默恢复默认地址。
- 增加 `null` 与空字符串配置的回归测试，确保不完整或损坏的配置会被拒绝，而不是意外使用 SMTP 或放宽代发权限。

## v1.1.1 - 2026-08-09

- 将配置面板的字段说明、LLM 工具说明和参数说明改为中文，便于中文环境直接配置和使用。
- 将 `README.md`、本更新日志、插件元数据和上游致谢完整中文化。
- 将 `owner_sender_ids` 与 `admin_sender_ids` 的默认值改为空列表，避免不符合 `platform_id:sender_id` 格式的示例值造成首次配置困惑。保留旧版 `YOUR_QQ_ID` 占位值的安全兼容处理。
- 明确 `qq_platform_names` 与 `/mailrelay_whoami` 返回的 `platform_id` 对应，并补充 QQ 号邮箱推导的可操作示例。
- 修复 `/mailrelay_send_test`：未填写真实 `owner_email` 时会拒绝发送，不再可能向示例地址投递测试邮件。
- 修复 SMTP 主机被显式清空时静默回退到 `smtp.163.com` 的行为，配置不完整时将拒绝投递。
- 修复未知布尔字符串被当作开启的问题，错误值会回退到各配置项的安全默认值。

## v1.1.0 - 2026-08-09

- 使用三种受策略约束的直接邮件工具替换仅草稿模式：固定主人投递、当前用户自助投递和仅管理员可用的第三方投递。
- 在共享 SMTP 投递边界执行收件人权限校验，不依赖面板命令权限或模型提供的参数。
- 增加隐私优先的 QQ/NapCat 自助邮箱解析，仅查询当前发送者可检测的资料；支持好友列表回退，不需要单独配置 NapCat URL。
- 增加私聊邮箱绑定、一次性验证码验证、`/mailrelay_unbind` 自助删除，以及默认关闭的 QQ 号邮箱推导。
- 增加带平台范围的主人和管理员白名单、包含失败的单用户尝试限制、成功限制、冷却时间、有限内存的限流记录，以及管理员投递专用收件人白名单。
- 完善直接投递模式的部署与隐私说明；补充网易 163 默认值，并为需要人工填写的配置提供示例值。
- 扩展直接工具授权、自助收件人隔离、绑定验证、NapCat 特性检测与限流的单元测试。
- 将声明的最低 AstrBot 版本提高到 4.25，确保重载或卸载插件时能正确清理直接 LLM 工具。

## v1.0.0 - 2026-08-09

- 独立发布首个版本，适用于 AstrBot 4.16 至 4.x。
- 增加网易 163 SSL 默认值：`smtp.163.com`、端口 `465` 和 `ssl`。
- 增加严格的精确邮箱和域名收件人策略，空白名单保持安全拒绝。
- 为控制命令增加双重授权：真实 AstrBot 管理员权限加发送者身份白名单。
- 增加 SMTP 连接测试、显式手动发送和固定格式测试邮件。
- 增加与会话绑定、会过期且必须经人工确认才能投递的 LLM 邮件草稿。
- 增加仅在成功后计数的原子限流、SMTP 超时、输入校验和隐私最小化 JSONL 审计日志。
- 增加单元测试、公开配置说明和上游致谢。
